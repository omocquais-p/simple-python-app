CREATE TABLE CUSTOMER (
                          ID VARCHAR(50) NOT NULL PRIMARY KEY,
                          FIRST_NAME VARCHAR(50) NOT NULL ,
                          LAST_NAME VARCHAR(50) NOT NULL ,
                          COMPANY_NAME VARCHAR(50) NOT NULL);

INSERT INTO CUSTOMER ('1', 'John', 'Smith', 'My company');